plugins {
    id 'java'
}

dependencies {
    implementation(project(':pure-gradle-api')) {
        because 'core依赖api接口定义'
    }
    testImplementation group: 'junit', name: 'junit'

    implementation 'com.google.guava:guava'
}
